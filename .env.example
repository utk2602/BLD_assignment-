PORT=3000
HOST=localhost
BROWSER_WIDTH=1365
BROWSER_HEIGHT=768

# Optional: only set this if Docker uses a non-default socket on your machine.
# Windows Docker Desktop example:
# DOCKER_SOCKET=//./pipe/dockerDesktopLinuxEngine

